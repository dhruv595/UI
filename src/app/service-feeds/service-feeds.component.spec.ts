/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ServiceFeedsComponent } from './service-feeds.component';

describe('ServiceFeedsComponent', () => {
  let component: ServiceFeedsComponent;
  let fixture: ComponentFixture<ServiceFeedsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ServiceFeedsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ServiceFeedsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
