/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ShowFeedsComponent } from './show-feeds.component';

describe('ShowFeedsComponent', () => {
  let component: ShowFeedsComponent;
  let fixture: ComponentFixture<ShowFeedsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ShowFeedsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ShowFeedsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
