/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { BookPatientDetailComponent } from './book-patient-detail.component';

describe('BookPatientDetailComponent', () => {
  let component: BookPatientDetailComponent;
  let fixture: ComponentFixture<BookPatientDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ BookPatientDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(BookPatientDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
