/*import { TestBed, inject } from '@angular/core/testing';

import { DrserviceService } from './drservice.service';

describe('DrserviceService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DrserviceService]
    });
  });

  it('should be created', inject([DrserviceService], (service: DrserviceService) => {
    expect(service).toBeTruthy();
  }));
});*/
